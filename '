{-# LANGUAGE OverloadedStrings #-}
module Main where

import Lib
import System.Environment (getEnv)

main :: IO ()
main = do
    path <- getEnv "PATH"
    let pathDirectories = splitPath path
    mapM_ print pathDirectories
